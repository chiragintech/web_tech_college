/*
function foo(x)
{
    return x*x;
}
foo(4);


Netscape Navigator - A famous old browser. Mosaic was there before this.
There was no functionality before - you had to send it to a data server where all the computation took place and then sent back the new data.
Livescript->JScript->ECMAsCRIPT
JS is an interpreted programming language.
Interpreted-JS,Python,Ruby
Compiled-Java,C/C++,Swift
"alert" displays an alert dialog with optional content and an OK button.
Syntax:window.alert("message");
Be careful with the quotes as it is same unlike in word. Also dont put spaces in between.
Convention ->double quotes>single quotes.
Data types -> number, string, boolean.
typeof(23);
'number'
*/
alert("helllo");